require StdMap;

operator entry() {
  Float32 a[Float32];
  a[5.34] = 9.12;
  a[-9.32] = 1.34;
  report("a = " + a);
  report("ReturnValue(a, -9.32) = " + ReturnValue(a, -9.32));

  // String b[][];
  // b.resize(2);
  // b[0].resize(2);
  // b[0][0] = "foo";
  // b[0][1] = "bar";
  // b[1].resize(2);
  // b[1][0] = "baz";
  // b[1][1] = "buzzy";
  // report("b = " + b);
  // report("ReturnSecondSecond(b) = " + ReturnSecondSecond(b));

  // report("ReturnStringArrayArray() = " + ReturnStringArrayArray());

  // report("ReturnDoublePtrArray() = " + ReturnDoublePtrArray());
}
