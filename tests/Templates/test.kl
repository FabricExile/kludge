require Templates;

operator entry()
{
  Template32 t1(1.2);
  report(t1.re());

  TemplateBool t2(false);
  report(t2.re());

  report(t2.constRef());

  InlineType i1();
  report(i1.getFooRe());

  //Myspace_MyspaceType1 mst1;
  Myspace_MyspaceType2 mst2;

  DerivedTempl dt;
  report(dt.re());
}

