require WrappedPtr;

operator entry() {
  Class value;
  report("ReturnClass() = " + ReturnClass());
  report("ReturnClassVec() = " + ReturnClassVec());
  value = ReturnClass();
  report("value.GET_m_pub() = " + value.GET_m_pub());
}
